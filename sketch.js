var a = 0;

function setup() {
  createCanvas(600, 600, WEBGL);
  normalMaterial
  var b = new Box(0, 0, 0, 200);
}

function draw() {
  background(0);
  stroke(255);
  noFill();
  translate(width / 2, height / 2);
  rotateX(a);
  b.show();
  a += 0.01;
}